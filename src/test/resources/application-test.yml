spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        format_sql: false
        dialect: org.hibernate.dialect.MySQLDialect

jwt:
  secret: test-secret-key-for-testing-only-minimum-32chars
  expiration-ms: 3600000

enrollment:
  max-credits: 18
  queue-multiplier: 100
