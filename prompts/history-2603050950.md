# 작업 이력 - 2026-03-05 09:50

## 요청
CyclicBarrier 추가로 동시성 테스트 동시 시작 보장

## 작업 내용
- `EnrollmentServiceConcurrencyTest.kt` 수정
- `CyclicBarrier` import 추가
- Test 1 (`concurrentEnrollment_onlyOneSucceeds`): `CyclicBarrier(threadCount)` + `barrier.await()` 추가하여 동시 시작 보장
- Test 2 (`concurrentEnrollment_thirtySucceed`): 동일 패턴 적용
- Test 3 (`concurrentEnrollment_sameStudent_creditLimitEnforced`): `startLatch`/`doneLatch` 패턴 → `CyclicBarrier` + `latch` 패턴으로 통일
- 3개 동시성 테스트 모두 통과 확인
