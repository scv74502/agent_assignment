# 작업 이력 - 2026-03-06 14:59

## 요청
이전 세션에서 수행한 "하드코딩 값 상수 분리 및 Redis 설정 외부화" 작업의 잔여 4개 항목 완료 및 커밋

## 작업 내용

### 1. EnrollmentQueueService - Duration 하드코딩 제거
- `Duration.ofHours(1)` → `Duration.ofHours(queueTtlHours)` 변경 (line 37)

### 2. .env.example - Redis 설정 추가
- `REDIS_LOCK_WAIT_TIME`, `REDIS_LOCK_LEASE_TIME` 섹션 추가
- `ENROLLMENT_QUEUE_TTL_HOURS` 섹션 추가

### 3. application.yml - Redis lock/queue 속성 추가
- `redis.lock.wait-time`, `redis.lock.lease-time` 환경변수 바인딩 추가
- `enrollment.queue-ttl-hours` 환경변수 바인딩 추가

### 4. application-test.yml - 테스트 설정 추가
- `redis.lock.wait-time: 5`, `redis.lock.lease-time: 10` 추가
- `enrollment.queue-ttl-hours: 1` 추가

## 검증
- `./gradlew compileKotlin` - BUILD SUCCESSFUL
- `./gradlew test` - BUILD SUCCESSFUL (ktlintCheck 태스크는 프로젝트에 미설정)
